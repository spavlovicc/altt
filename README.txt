To create images and run containers run:
init.sh

Response is default GET flask response.
Counting of visits can be done with access.log parsing.
Container name can be accessed with /etc/hostname in container.
